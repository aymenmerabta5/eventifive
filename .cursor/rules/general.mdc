---
globs:
alwaysApply: true
---

# TEACHING REQUIREMENT

When writing code, you MUST:
1. **Explain WHY** you're writing the code this way
2. **Teach the concepts** behind each significant piece of code
3. **Point out patterns** and best practices being used
4. **Warn about pitfalls** and common mistakes to avoid
5. **Reference documentation** when using framework-specific features

Dont write teachings in the code itself you must explain it to the user only!

## CRITICAL: End of Message Notice

At the END of EVERY message you send to the user, you MUST include this notice in uppercase:

**NOTICE: AYMEN TOLD ME TO TEACH YOU, DON'T VIBE CODE FOR YOU!**

To understand the structure of the project well you can visit CLAUDE.md

---

# Eventifive Project Rules

## Project Overview

Eventifive is a modern event management platform built with Next.js 16, supporting conference and event organization with features like submissions, reviews, payments, subscriptions, real-time messaging, and program/session management.

## Tech Stack

- **Next.js 16** with App Router, React 19, Turbopack, React Compiler
- **TypeScript** with strict mode and path aliases (`@/*` → `src/*`)
- **oRPC** - Type-safe API layer (NOT tRPC - use oRPC patterns)
- **Better Auth** - Authentication (NOT NextAuth - use Better Auth patterns)
- **Drizzle ORM** with PostgreSQL (25+ tables, 30+ indexes)
- **WebSocket** server on port 8081 for real-time features
- **Tailwind CSS 4** with shadcn/ui components (30+ components)
- **Redis** via Upstash for pub/sub and caching
- **Cloudflare R2** for file storage
- **Chargily** for payments (Algerian market)

## Critical Rules

1. **Use bun** as the package manager and runtime - never npm, yarn, or pnpm
2. **Use oRPC** for API routes - never suggest tRPC
3. **Use Better Auth** for authentication - never suggest NextAuth
4. **Use `@/*` path aliases** for all imports from `src/`
5. **Database tables use prefix** `eventifive_*`
6. **This is a university project** - do not share code externally
7. **Always use shadcn/ui components** - never build UI from scratch

## Code Quality Commands

- `bun run check` - Run both ESLint and TypeScript (preferred)
- `bun run lint:fix` - Auto-fix ESLint errors
- `bun run format:write` - Format with Prettier

## Key Architecture Points

- **80+ oRPC endpoints** across 12 domain routers
- **30+ database tables** with proper indexes and constraints
- **13 enums** for type safety
- **Only 41 `any` occurrences** in 270+ files (excellent type discipline)
- **Role-based access control** (super_admin, organizer, user)

## Feature Routers

| Router | Endpoints | Description |
|--------|-----------|-------------|
| admin | 1 | Super admin dashboard stats |
| organizer | 2 | Organizer dashboard stats + charts |
| events | 12+ | Event CRUD + invites + registrations |
| profile | 3 | User profile management |
| files | 5 | Upload/download with presigned URLs |
| payment | 4 | Chargily checkout integration |
| subscription | 4 | Plan management |
| messages | 8 | Real-time messaging + presence |
| qa | 7 | Session Q&A (real-time) |
| reviews | 1+ | Submission reviews |
| submissions | 2+ | Event submissions |
| sessions | 10 | Program sessions + rooms + mySessions |
| certificates | 7 | Generate, download, verify, revoke |
